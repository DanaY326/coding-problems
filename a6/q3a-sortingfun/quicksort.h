// quicksort.h
// A module that performs quicksort on an array

// quick_sort(arr, arr_lenn) performs quicksort on arr
// requires: arr must be valid, arr_len must be positive
// effects:  might modify arr
void quick_sort(int *arr, int arr_len);
